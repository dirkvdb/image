box: dirkvdb/cxx14
no-response-timeout: 10
build:
    steps:
        - script:
            name: Setup environnement variables
            code: |
                SRC_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
                BUILD_DIR="${SRC_DIR}/build"
                echo "SRC_DIR ${SRC_DIR}"
                echo "BUILD_DIR ${BUILD_DIR}"

        - script:
            name: Install dependencies
            code: |
                sudo apt-get install libxdg-basedir-dev libpng-dev libjpeg-dev

        - script:
            name: Compile Image
            code: |
                mkdir -p "${BUILD_DIR}"
                cd "${SRC_DIR}"
                git clone https://github.com/dirkvdb/utils.git ../utils
                cd ../utils
                git submodule update --init --recursive
                git checkout develop
                cd "${BUILD_DIR}"
                cmake -DSTANDALONE=1 -DCMAKE_BUILD_TYPE=Release "${SRC_DIR}"
                make -j8
                make check -j8

        - script:
            name: Run Tests
            code: |
                cd "${BUILD_DIR}"
                make test
